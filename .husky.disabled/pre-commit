# Husky v9+ hook (no bootstrap lines needed). Keep commands minimal & fast.
# 1. Lint & format only staged files
bunx lint-staged

# 2. (Optional) Type checking across workspace. Comment out if too slow.
bun run type-check